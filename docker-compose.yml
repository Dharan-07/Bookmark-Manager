services:
  postgres:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: bookmark_user
      POSTGRES_PASSWORD: dharan_0702
      POSTGRES_DB: bookmark_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: